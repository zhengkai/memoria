生成最终静态页面

思路：

先快速启动，承认所有请求，以所有已存在的页面为准，有就返回，没有就 404  
（目前这一步本地 18ms，线上接近 1s）

然后根据 export 的 data，重新生成一遍所有页面，xattr hash 相同就跳过，  
不同就换成新的

小文件直接进内存，大文件保存路径

每个页面分 raw / gzip / br 三种，都使用相同的 hash，etag 就是 hash
